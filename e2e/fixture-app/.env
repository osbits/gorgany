MODE=prod
SERVER_PORT=8080
APP_GREETING=hello-from-env

DB_HOST=postgres
DB_PORT=5432
DB_USER=gorgany
DB_PASSWORD=gorgany
DB_NAME=gorgany_e2e
DB_SSL=disable

# At least auth.MinJwtSecretLength bytes of varied material: the framework refuses to sign or
# verify with anything weaker, so a short fixture value stops this app booting at all.
JWT_SECRET=fixture-e2e-jwt-signing-key-8f3a2c91d4b7e6
